# Copia este archivo como `.env.development` (no se versiona) o exporta la variable
# antes de `npm run dev`. Sin ella, el cliente HTTP usa la URL de desarrollo por omisión
# declarada en src/comunes/api/configuracionApi.ts.
VITE_URL_BASE_API=http://localhost:5122/api
