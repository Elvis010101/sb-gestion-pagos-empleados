# ---------------------------------------------------------------------------
# .NET
# ---------------------------------------------------------------------------
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
[Ll]og/
[Ll]ogs/
*.log

# Resultados de pruebas y cobertura
[Tt]est[Rr]esult*/
*.coverage
*.trx
coverage*.json
coverage*.xml
coverage*.info

# Artefactos de Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo
*.userprefs
*.DotSettings.user

# NuGet
*.nupkg
packages/
.nuget/

# ---------------------------------------------------------------------------
# Secretos y configuración local
# El appsettings.json versionado lleva un placeholder; la cadena de conexión real
# (con contraseña) vive solo en appsettings.Development.json, que NO se sube.
# El README documenta cómo crearlo.
# ---------------------------------------------------------------------------
appsettings.Development.json
appsettings.*.Local.json
*.pfx
.env
.env.*
!.env.example

# ---------------------------------------------------------------------------
# Node / React / Vite
# ---------------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
build/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# ---------------------------------------------------------------------------
# Documentos de trabajo del candidato
# Notas de proceso, no entregables. Lo que de ellos importa para la entrega ya está
# volcado en el README (supuestos, decisiones y alternativas descartadas).
# La barra inicial ancla el patrón a la raíz: un PLAN.md dentro de una subcarpeta
# sí se versionaría, que es lo correcto para un patrón tan genérico.
# ---------------------------------------------------------------------------
/PLAN.md
/Notas.md

# ---------------------------------------------------------------------------
# Sistema operativo
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
*~

# Salida del registro de Serilog
Registros/
